#include "vehicle.h"

int main() {
    std::cout << "Ferry goes brrrrr";
    Vehicle(12, 12, 12);
    std::vector<Vehicle> vehicles;
    vehicles.emplace_back(12, 12, 12);
    vehicles.emplace_back(12, 12, 12);
    vehicles.emplace_back(12, 12, 12);
}